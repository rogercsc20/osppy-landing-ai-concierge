# Sitio v6 · 03 · Cierre

> **Fecha: 2026-09-08 (noche).** Repo del sitio, `docs/`. Español (HQA-D3).
> **Qué es:** el cierre del sitio v6, tal como lo pide el prompt del 2026-09-05 §11.6 y el prompt de cierre del
> 2026-09-08 §3 B. Lo medido con sus números, las compuertas decididas, lo podado, lo que queda abierto con su
> dueño y la fila propuesta para `ROLES.md`.
> **Lo que este documento NO es:** una aprobación. La compuerta 3 la cerró el operador con su palabra esa noche
> (fila del ledger), no un número de este documento.

## 1. Dónde quedó

En vivo en www.osppy.com, `main` en `cd3a8c0`, empujado siete veces ese día con su palabra, una por una y con el
hook de push saltado solo para cada push (el séptimo, HQA-D172, llevó a producción los cinco commits de esta fase;
verificado después del despliegue: `/photos/hero.webp` responde 404, seis rutas responden 200 en los dos idiomas y
doce capturas del sitio en vivo no reportan un solo error de consola). La casa en `/es` y `/en`, ocho páginas de área, ocho de industria, el
carrusel, las 24 tendencias tecnológicas con su fuente, el contador de cifras y la vista previa del enlace con el
logo oscuro. La compuerta 3 quedó cerrada con la respuesta del operador a la pregunta de las buenas manos.

## 2. Lo medido

Contra `npm run build && PORT=3005 npm run start` en esta máquina, el 2026-09-08 por la noche. Los números son
los que salieron, sin adjetivos.

**JavaScript de primera carga** (`scripts/first-load.mjs`, tamaño codificado, presupuesto 200 kB):

| Ruta | Primera carga | Con diferidos |
|---|---|---|
| `/es` | 142.7 kB en 6 archivos | 142.7 kB en 6 |
| `/en` | 142.7 kB en 6 archivos | 142.7 kB en 6 |
| `/es/estrategia` | 136.5 kB en 6 archivos | 136.5 kB en 6 |
| `/es/industrias/manufactura` | 142.3 kB en 6 archivos | 142.3 kB en 6 |

Ninguna ruta carga nada después de la hidratación: no hay `next/dynamic` en el sitio.

**Lighthouse móvil** (`scripts/lighthouse.mjs`, `npx lighthouse`, dos corridas):

| Ruta | Rendimiento | Accesibilidad | FCP | LCP | TBT | CLS |
|---|---|---|---|---|---|---|
| `/es` | 90 y 87 | 100 | 0.9 s | 3.3 s y 4.0 s | 100 a 160 ms | 0 |
| `/en` | 87 y 87 | 100 | 0.9 s | 4.0 s | 70 ms | 0 |
| `/es/estrategia` | 90 y 90 | 100 | 0.8 s | 3.5 s | 100 ms | 0 |
| `/es/industrias/manufactura` | 90 y 90 | 100 | 0.8 s | 3.6 s | 90 ms | 0 |

El elemento del LCP es el `<h1>` del recibimiento, no la fotografía: espera a la tipografía. Lighthouse marca
53 KiB de JavaScript sin usar y 13 KiB de JavaScript heredado; la meta del script (90) queda dentro del margen
de variación entre corridas en la casa. Nadie tocó el diseño para subir el número: el criterio de aceptación es
la compuerta 3.

**Capturas** (`scripts/capture.mjs`, servidor de producción):

- 24 combinaciones: `/es`, `/en`, `/es/estrategia`, `/es/industrias/manufactura` × 390, 1024 y 1440 px × claro
  y oscuro. Cero desbordes horizontales, cero errores de consola. `captures/v6-cierre/`.
- 8 combinaciones más con reducción de movimiento: cero elementos de texto ocultos. `captures/v6-cierre-reducido/`.

**Compuertas y pruebas:** `npm run check` (260 llaves, 0 fallas, 0 avisos, secciones y contraste en verde),
`npm run lint` limpio, `npm run build` con 39 páginas prerrenderizadas, `npm run test` con las ocho pruebas en
verde. La integración continua de GitHub sigue en rojo por una razón que no es el código: le falta el secreto
`HQ_READ_TOKEN` para poder leer el banco de palabras de hq (dueño O).

### 2.1 Calidad de las fotografías (HQA-D168, ejecutado el 2026-09-08 a las 21:17)

El operador dijo que las imágenes se veían de mucha peor calidad. No se había degradado ninguna: la tubería
escribía WebP a calidad 78 con un tope de 1600 px de lado largo (2400 en las filas con `ladoLargo`), y el
optimizador de Vercel volvía a comprimir a AVIF con la calidad 75 que pide `next/image` por omisión. Una foto a
sangre a 1440 puntos en una pantalla de densidad doble necesita 2880 px reales, así que cinco fotos de área
mostradas a todo lo ancho entregaban 1600.

La tubería quedó en **3200 px de lado largo y calidad 82**, y las páginas que muestran una foto grande le piden
**calidad 90** al optimizador (`images.qualities: [75, 90]` en `next.config.ts`, que Next 16 exige declarar, más
`quality={90}` por omisión en `Photo` y en los dos componentes que usan `next/image` directo). Las catorce filas
que traían `ladoLargo` 2400 lo perdieron, porque con el tope en 3200 las frenaba desde abajo; el campo se queda
como mecanismo de excepción.

Medido con `curl` sobre `/_next/image`, no con el navegador: `img.naturalWidth` devuelve píxeles CSS cuando la
imagen salió de un `srcset` con descriptores `w`, y reporta 900 donde la imagen mide 2400.

| Lo que recibe una pantalla de 1440 puntos a densidad doble (AVIF) | Antes (q=75) | Después (q=90) |
|---|---|---|
| `recibimiento`, el recibimiento a sangre | 2400x1738 · 78 kB | 3200x2317 · 169 kB |
| `capacitacion`, foto de área a sangre | 1600x1067 · 62 kB | 3200x2133 · 221 kB |
| `contabilidad`, foto de área a sangre | 1600x1140 · 37 kB | 3200x2133 · 170 kB |

| Otras mediciones | Antes | Después |
|---|---|---|
| `public/photos` (21 fotografías) | 3.9 MB | 9.9 MB |
| Lighthouse, rendimiento en las cuatro rutas | 85 · 88 · 90 · 90 | 83 · 84 · 89 · 88, y 86 · 87 en una segunda corrida de `/es` y `/en` |
| Lighthouse, accesibilidad | 100 en las cuatro | 100 en las cuatro |
| JavaScript de primera carga | 136.5 a 142.7 kB | 136.5 a 142.8 kB |

La caída de rendimiento cabe en la variación entre corridas que esta misma sección ya documenta, y ninguna ruta
perdió los cinco puntos que habrían obligado a bajar la calidad. Las compuertas, el lint, el build y las ocho
pruebas siguen en verde. Dos originales no llegan a 3200 y `withoutEnlargement` los deja en su tamaño nativo:
`hablemos` (2400, se muestra a 568 puntos) y `comercio-contenedores` (3008, suficiente para 1440 a densidad
doble). Los `sizes` se revisaron uno por uno y ninguno declara una caja menor que la real, así que no se tocaron.

**La fotografía del área Datos cambió.** Era el tercer original que no llegaba a 3200 (topaba en 2426) y además
era una laptop con un panel de cifras, sin personas: contra dos reglas del §2 del sistema visual. El operador
eligió, entre tres candidatas presentadas en la caja real de la página, dos colegas leyendo juntos un reporte de
gráficas impreso (Yan Krukau en Pexels, 7693745, 6000x4000). Se descartaron una pizarra que traía la marca
StockCharts.com a la vista y un par de dashboards de laptop.

## 3. Las compuertas, decididas

Ninguna se obedeció a ciegas ni se borró en silencio. El detalle vive en `docs/2026-09-08-v6-02-sistema-visual.md`
§6; aquí está lo que se decidió y por qué.

- **`check-sections.mjs`: reescrito.** Su regla era de la v2 ("ninguna sección pinta su propia banda") y este
  sitio tiene bandas por decisión del operador (HQA-D149, D154), así que la regla era falsa y no disparaba nunca.
  Se reescribió a la regla del §3.3 que sí describe el sitio y que un defecto real probó: una `<section>` no
  recorta (`overflow-hidden` y sus hermanas) ni fija su altura (`min-h-*`); eso va en un `div` adentro. Un
  `overflow-hidden` heredado fue lo que apagó el panel pegajoso de "Cómo ayudamos" (HQA-D159). Verificado que
  muerde: una sección con esas clases lo hace salir con código 1.
- **`capture.mjs`: afinado, con la nota fechada dentro de la medición.** Contaba como "oculto bajo reducción de
  movimiento" a las ocho pistas "Ver cómo ayudamos", que descansan en opacidad 0 y aparecen al pasar el cursor
  o al enfocar. Eso no es un reveal de movimiento, es una afordancia, y se ve igual con el movimiento apagado.
  Ahora un elemento que declara las dos revelaciones no cuenta, y el que solo declara la del cursor sí, porque
  un lector con teclado nunca lo vería. Verificado que muerde: quitándole la variante de foco, vuelven a contar 8.
- **`check-copy.mjs` y `check-contrast.mjs`: sin cambios.** Ya llevaban su política desde `4061752` y `1e23ee2`.
- **Rutas por defecto de `capture.mjs`, `lighthouse.mjs` y `first-load.mjs`:** eran las de la v5, borradas hace
  días, así que una corrida sin argumentos medía la página de no encontrado. Ahora son las de la v6.

## 4. Lo podado

- **Fotos.** El manifiesto pasó de 35 filas a 21, que es exactamente lo que el sitio renderiza (medido leyendo
  `foto` en `data/` y `messages/`, y `slug="…"` en `components/` y `app/`). Salieron trece fotos heredadas de las
  áreas de la v5 y `hero`, la única imagen generada con un modelo, con sus `.webp`. El campo `_` del manifiesto
  ya no dice que una imagen generada pueda quedarse para ningún fin (fundamento §9, HQA-D137). `public/photos`
  pesa 3.8 MB en 21 archivos.
- **Dependencias (HQA-D138 delega la decisión en la sesión).** Se retiraron 24 dependencias directas, todas sin
  un solo `import` en el repo, en cuatro tandas con `build` y pruebas después de cada una:
  animación (`gsap`, `@gsap/react`, `lenis`, `motion`, `@number-flow/react`), gráficas (los ocho `@visx/*`,
  `d3-array`, `d3-shape`), interfaz heredada de shadcn (`class-variance-authority`, `clsx`, `tailwind-merge`,
  `lucide-react`, `radix-ui`, `shadcn`, `tw-animate-css`) e infraestructura no cableada (`next-intl`, que no se
  usa porque el sitio tiene su propio `lib/i18n.ts`, y `@vercel/analytics`, que nunca se importó).
  El lockfile pasó de 829 a 432 paquetes. Quedan tres dependencias de ejecución: `next`, `react` y `react-dom`.
  `lucide-react` se fue aunque el sitio dibuje iconos: los dibuja a mano.
- **Archivos.** `lib/utils.ts` (el `cn` de shadcn, sin un solo uso) y `types/react-view-transition.d.ts` (nadie
  usa `<ViewTransition>`) salieron con la carpeta `types/`.
- **Si algún día vuelve a hacer falta.** Medir movimiento antes de instalar: el sitio anima dos cosas (color en
  botones y enlaces, y la foto del área al pasar el cursor) más el contador de cifras de HQA-D165, y las tres
  son CSS o veinte líneas de JavaScript. `@vercel/analytics` es distinto: no es una dependencia huérfana por
  error, es una decisión que nadie ha tomado. Medir el tráfico del sitio es una fila del operador, y el día que
  la tome son un `npm install` y un componente en el layout.

## 5. Accesibilidad: dos arreglos de esta fase

- **El contador de cifras.** Llevaba el texto final en un `aria-label` sobre un `<p>` o un `<dt>`, elementos sin
  un rol que admita nombre accesible, así que Chrome lo ignoraba y el árbol de accesibilidad leía "Solo el 0%"
  mientras la cifra no había entrado en pantalla. Ahora el texto final vive en un `<span>` visualmente oculto y
  el animado está fuera del alcance de las ayudas técnicas: lo que se anuncia es siempre la cifra real. Es la
  intención de HQA-D165 bien implementada, no una decisión reabierta.
- **Las pistas "Ver cómo ayudamos"** ya tenían su variante de foco, así que un lector con teclado sí las ve. Lo
  que estaba mal era la medición, y se afinó (§3).

## 6. Lo abierto, con dueño

**Del operador (O):**

- El secreto `HQ_READ_TOKEN` en GitHub. Sin él la integración continua se queda en rojo porque no puede leer el
  banco de palabras de hq (HQA-D156).
- El número público de WhatsApp (HQA-D133). Hoy el sitio solo da `hello@osppy.com`.
- La bio de Instagram con la frase de HQA-D140.
- Las fotos reales de Osppy trabajando. El día que existan entran por el mismo pipeline y sustituyen una por una,
  empezando por el recibimiento (§3.5 del sistema visual).
- La revisión del banco de palabras prohibidas (HQA-D115), pendiente desde antes de este sitio. "Experto" vive
  hoy en el llamado a la acción por HQA-D148, con nota fechada en el banco y sin tocar la fila.
- Medir el tráfico del sitio, si lo quiere (§4).

**De la sesión (E):**

- El LCP de la casa (3.3 a 4.0 s en Lighthouse móvil) lo marca el `<h1>`, que espera a la tipografía. Se puede
  mirar sin tocar el diseño aprobado.
- Los 53 KiB de JavaScript sin usar que reporta Lighthouse, sin abrir nada de HQA-D148 a D167.

**Del operador (O), abierto el 2026-09-08 a las 21:17 con la regeneración de las fotos:**

- A 3200 px quedaron más nítidas dos marcas que ya se veían a 2400: el logo de Apple en la laptop de la
  fotografía de Estrategia y una placa en cirílico en la de manufactura. La HQA-D163 prohíbe logos y marcas de
  empresa a la vista en una fotografía que representa una industria, que alcanza a la de manufactura; la de
  Estrategia es de área. Si decide cambiar alguna, el reemplazo entra por el mismo camino que el de Datos.

## 7. Fila propuesta para `ROLES.md`

El rol con el que se hizo este sitio no existe en `ROLES.md`. Esta es la fila, ajustada a lo que la sesión
realmente hizo (el apéndice A del prompt del 2026-09-05 la proponía antes de construir):

> **R-WS · Sitio (redactor, arte y front).** **When:** la historia de Osppy y cualquier copy que diga qué es la
> empresa; el sitio público: estructura, copy, sistema visual, construcción, medición y las decisiones de sus
> compuertas; este prompt y sus sucesores. **Entry pack:** `CLAUDE.md`; el fundamento de marca completo; oferta
> §1–§2 y oferta-reglas §4; FDV §2, §9–§10, §11.1, §11.10–§11.12; `pricing.md` §Hard rules y §6;
> `banco-prohibido.md` §2–§3; en el repo del sitio, `docs/2026-09-08-v6-01`, `-02`, `-03` y `-04` (≈ 115 KB,
> declarado sobre presupuesto: escribe, diseña y construye de las mismas fuentes). **Forbidden:** el sitio
> borrado (`v5-cierre-2026-09-05`) como fuente; la entrevista fundacional como fuente de frases; la guía §1–§6 y
> §8; `customers/*`; generar imágenes con un modelo; nombrar un cliente; publicar una cifra que no esté en
> `scripts/copy-allow.json` con su enlace y su fecha de lectura. **Tools:** `npm run check`, `lint`, `build`,
> `test`; `scripts/capture.mjs`, `lighthouse.mjs`, `first-load.mjs` y `optimize-photos.mjs` sobre un servidor de
> producción; git. **Writes · naming:** `../osppy-hq/business/marca/AAAA-MM-DD-historia-de-osppy.md`; en el repo
> del sitio `docs/AAAA-MM-DD-v6-NN-<tema>.md`, `messages/*.json`, `data/*.json`, `app/`, `components/`,
> `scripts/`; en hq solo filas del ledger y la fila Landing del Track 5. **Done:** §13 del prompt del 2026-09-05,
> con la compuerta 3 dicha por el operador con sus palabras. **Operator decides:** cualquier cifra, giro,
> cliente o palabra del banco; cada push; el diseño en cada parada. **Language:** español.

## 8. Qué hace la siguiente sesión primero

El prompt de la calidad de las fotografías (HQA-D168, escrito por otra sesión esa misma noche): regenerar el banco a
3200 px con calidad 82 y pedirle al optimizador de Next calidad 90. Corre sobre el manifiesto ya podado de esta fase,
que son las 21 fotos que el sitio de verdad renderiza. Además, recordarle el secreto `HQ_READ_TOKEN`, que es lo único
que mantiene la integración continua en rojo. Fuera de eso, nada del sitio está pendiente: lo que siga es trabajo
nuevo y empieza por una conversación, no por un parche.

---

*Sitio v6 · 03 · Cierre · 2026-09-08 · los números son de esta máquina, no de producción · la compuerta 3 la
cerró el operador, no este documento.*
