/**
 * The site-wide motion vocabulary: one easing, one reveal duration.
 * Springs are reserved for the chat bubbles (they read as live messaging).
 * Mirrors --ease-luxe in app/globals.css.
 */
export const EASE_LUXE = [0.22, 1, 0.36, 1] as const;
export const DUR_REVEAL = 0.8;
